# 更新记录

每个版本改了什么，写给正在决定要不要装的人看。

本文件是 GitHub Release **中文**正文的来源；英文仍见 [CHANGELOG.md](CHANGELOG.md)。
Sparkle 更新窗口继续只用英文 CHANGELOG.md —— 见 [Scripts/release-notes.py](Scripts/release-notes.py)。
在打 tag 之前写好 `## x.y.z` 条目；语法与英文相同：列表、`**粗体**`、`` `代码` ``、`[链接](https://example.com)`。

## 1.1.0

- **本仓库（[harrisliangsu/Pulse](https://github.com/harrisliangsu/Pulse)）为更新源。** Sparkle 的 appcast 与 GitHub Releases 指向本仓库，而不是上游 qunqin24/Pulse。安装自本仓库的副本会从这里收到后续版本。
- **大额余额不再撑破圆环。** 胶囊显示 ¥5k、¥123k、$1.2M 这样的缩略式，而不是装不下、被裁切的完整数字 —— 精确金额在卡片和设置页。永远**向下取整**，圆环绝不会多报你还有多少。
- **胶囊不再一开始就略偏低、碰一下才归位。** 多数显示器上面板比 macOS 肯给的空间更高，Pulse 是按窗口**实际**落到的位置来算胶囊该画在哪的。它问得太早 —— 窗口还没上屏，答案仍是**请求**的位置 —— 所以胶囊画在了该在位置下方约 76pt，第一次改任何设置才跳回正确位置。
- **API 余额查得更勤了。** Pulse 靠盯着这台 Mac —— 有 agent 在写 transcript、有数字动了、你瞟了一眼胶囊 —— 来调节节奏，所以忙时勤、闲时静。但 API 上花的钱在本机留不下痕迹，DeepSeek 和 Command Code 就一直被晾满半小时：它等是因为没变，没变是因为它在等。这两家现在至少每五分钟查一次。其余服务商不受影响；低电量或藏起面板时仍会安静；你自己设的固定间隔仍是那个意思。
- **服务商列表默认按字母排序。** 以前是按几个月来陆续加进去的顺序，对读者没有意义。你自己排过胶囊顺序的，原样保留。
- **余额快没了告诉我一声。** 卖预付余额的服务商 —— DeepSeek 和 Command Code —— 各自设置里有一条**余额提醒线**，跌破时提醒一次。用金额不用百分比，因为这两家没有可拿来算百分比的额度；按服务商各设一条，因为 ¥20 和 $20 不是同一条线。不填就不提醒，和其他通知一样。说一次，充值前不重复 —— 或你挪动了那条线，那是新问题，再答一次。
- **预付余额相关通知说得更少，也说得对了。** 买来的余额不会重置，也不能靠算术宣称花光，所以改圆环按什么计量不再误报「限额已重置」；余额达到**你**自设数字的 100% 也不再声称服务商说你没钱了。只有服务商自己这么说时才这么说。
- **DeepSeek 作为预付余额服务商加入**，也是 Pulse 里第一个完全不报告额度的 —— `GET /user/balance` 只说还剩多少预付余额，别无其他。没有配额、没有窗口、没有消费历史可读，圆环的分母得从别处来，在 DeepSeek 设置里选。**自上次充值**是默认且无需填写：Pulse 记住它见过的最高余额，余额往上走只能是充值，圆环就从满格重新开始。**只看余额**完全不画环，金额直接上胶囊。**我的预算**按你填的数字量。刚开始用「自上次充值」前两天读数会偏低 —— Pulse 只能从它开始盯的那一刻量起，卡片会标明是哪一天。
- **智谱那一行现在叫「Zhipu」。** 胶囊和设置列表其余都是拉丁字母，单独一行汉字读起来像另一种东西而不是又一家店。公司、站点、密钥都不变 —— 只改这一行的名字。其同伴仍叫 **z.ai**，那是那家公司自己的拼写。
