# Testing

Owns: what `swift test` covers, what it deliberately does not, and the two conventions the suite depends on. Toolchain and build flags: [build-from-source.md](build-from-source.md).

```bash
swift test
```

There was no test target until 2026-09-07. What prompted one was not a policy: two real faults were found by reading code in a single afternoon — Antigravity taking the first matching helper and giving up when the one that answers was second, and `.stale` being treated as a failed fetch when a *successful* one produces it too — and the notification rules had shipped with nothing proving them at all.

## What is covered

| Suite | Covers |
|---|---|
| `AlertMemoryTests` | Every notification rule: thresholds, spent, resets, the failure streak, the stale-age gate, and the low-balance line — said once, re-armed by a top-up, said again when the line moves, and never from a stale reading or a balance with no figure behind it. [notifications.md](notifications.md) |
| `AlertsThroughTheCacheTests` | The same rules reached the way production reaches them: service → `UsageCache.reconciled` → state machine |
| `NotificationAuthorizationTests` | An injected authorization decision, concurrent requests, no warning consumed while a grant is pending, and the foreground delegate selector; no system permission or notification delivery |
| `UsageCacheTests` | `reconciled` — fallback, "a reading never goes backwards", credentials that must not be papered over, expiry, and that a live reading carrying a balance and no limits is an **answer** rather than a failure to paper over (DeepSeek's "balance only"), while one carrying nothing at all still is |
| `UsageWindowTests` | The reported figure at both ends; when the window clock may divide; elapsed and remaining clock boundaries; and the persisted direction's default, round trip and invalid-value fallback |
| `UsageTintTests` | The configurable amber→red boundary, including exact-threshold equality, the fixed green boundary, and spent overriding the setting. [ui/rings-and-surface.md](ui/rings-and-surface.md) |
| `ClaudeCodeSeverityTests` | Warning severities leave room; locked reasons and unknown severities still mark the limit spent. [providers/claude-code.md](providers/claude-code.md) |
| `RailGeometryTests` | Every ring the rail draws is reachable: each centre inside the rail, the last ring whole rather than clipped, each ring hit-testing to its own slot — across left/right/top and docked/floating. Plus the count itself: `shownSlotCount` is called with a reading that really splits, so a window measuring its rects in accounts rather than rings fails a test instead of a click (verified by reintroducing the bug) |
| `NotchGeometryTests` | Public-screen-area geometry including offset displays; physical-top placement with controls below the housing and unchanged non-notch positioning; empty collapsed surface, the warning edge directly below and inside the housing, straight full-width sides at the screen top, minimum housing width, unchanged ring containment, and padding/card/window budgets across every panel scale, both top-label settings, and both end styles. These are geometry checks, not real-input verification. |
| `RailOffsetTests` | The rail's offsets measured against the frame the window was **granted**, not the one it asked for — the panel is taller than a laptop's usable screen and AppKit refuses that frame. [ui/panel-geometry.md](ui/panel-geometry.md) |
| `ZaiHistoryReadTests` | What a history read found *out*: no key is not a failed request, and a successful reply with no rows is an answer |
| `ActiveDisplayTests` | Following the pointer onto another display: the move keeps dock and both ratios, a held panel refuses and the refusal is *reported* so the display stays on offer, and returning to the display it is already on is quiet rather than a refusal. [ui/panel-geometry.md](ui/panel-geometry.md) |
| `PanelHoldTests` | Nothing re-places the panel while it is held — including the gap between mouse-down and the first movement, where `isDragging` is still false. [ui/input.md](ui/input.md) |
| `RefreshPacingTests` | Automatic cadence, the unwatched-spend cap, and the timer being the only caller that skips providers not yet due. The production batch-result comparison includes Devin and every other represented provider; timestamps alone and an empty batch do not count as usage changes. [refresh-and-data.md](refresh-and-data.md) |
| `AgentActivityTests` | Scans only selected transcript providers, with synthetic logs in a temporary home. The store passes enabled accounts through to the monitor, including added accounts; no local provider means no scan. Held readers cover cancellation and late results across selection changes and stopping. Panel visibility and observed turn completion are checked without provider requests or real transcripts. [refresh-and-data.md](refresh-and-data.md) |
| `ProviderMarkTests` | Every provider's mark loads **and puts ink down** at the size the rail draws it. `LobeIconStore.image(named:)` returns non-nil for any file `NSImage` can open, so an SVG that parses but renders blank becomes a valid template image and a blank disc on the rail, failing nothing. The floor is 8% coverage against a bundled set that runs 22.4–52.7%. Verified by blanking a mark and watching it fail. [ui/rings-and-surface.md](ui/rings-and-surface.md) |
| `RailSlotTests` | The rail's order — including the order before anybody arranges it, which is by name, and that an arrangement somebody made is not re-sorted under them: when a split account becomes two slots, when it stays one, and that unscoped windows never form a group. [ui/rings-and-surface.md](ui/rings-and-surface.md) |
| `AntigravityParsingTests` | A captured `RetrieveUserQuotaSummary` reply → `[UsageWindow]` |
| `VendorPriceTests` | Pricing a model only a plan vendor publishes: first-party rates win, vendor rates require that vendor, and unknown models stay unpriced. Exercises normalized records and the production OpenCode/Kilo database dispatch through day, slot, session and model amounts; a pre-vendor cache is an offline fallback only. [token-spend.md](token-spend.md) |
| `MyriadUnitsTests` | Which languages count by 10⁴/10⁸ and in whose characters — 亿 / 億 / 억 are not interchangeable, and a region-only `zh_TW` still reads as traditional. Silent failure: the figure is arithmetically right and spelled for the wrong country. [development.md](development.md) |
| `GlobalShortcutTests` | What may be bound to a key: a real modifier is required (⇧ alone and bare keys are refused), modifiers outside the four are dropped, storage round-trips, and storage that no longer parses reads as *no* shortcut rather than as some other key. [ui/input.md](ui/input.md) |
| `MenuBarIconSettingTests` | Dedicated persistence callbacks, status-menu rebuilding, and the entry-point rule: with the panel hidden and no shortcut accepted by the window server, the menu bar icon must remain. [architecture.md](architecture.md#ways-into-settings) |
| `NetworkProxyTests` | System as the upgrade-safe default, persistence, host/port validation, URLSession proxy configuration without a live request, and exact HTTP/SOCKS helper environments including loopback exclusions. [networking.md](networking.md) |
| `ProviderSelectionTests` | Fresh installs and dismissed setup stay empty regardless of detection; valid choices and extra-only rails survive; legacy absent versus empty/malformed keys; newly supported detected services are suggested once and never auto-enabled; discovery through an existence-only seam; empty-selection guards on the production store's startup, refresh and Codex history entry points; a per-account refresh refuses disabled providers. No real provider or Keychain access. [architecture.md](architecture.md#provider-choice-before-monitoring) |
| `KiroUsageTests` | Captured ACP usage mapping, malformed-pool rejection, provider-specific failures, the bundled mark, and resource-type window identities surviving an array reorder. [providers/kiro.md](providers/kiro.md) |
| `UsageReportTests` | The `--json` shape, which is a contract other people build on. [json-output.md](json-output.md) |
| `ConnectionDiagnosticTests` | Failures through real cache reconciliation, persisted origin, legacy unknown sources, capture freshness versus cache selection, allowlisted diagnostic copy, account-specific repair choices, and credential failures at pinned service boundaries without network calls |
| `PulseLinkTests` | Encoded added-account ids, malformed/action-bearing URL rejection, existing-account navigation and repeated-link requests |
| `VolcengineSignerTests` | Volcengine's request signature, cross-checked against a second implementation |
| `ZaiHistoryTests` | The statistics endpoint's shape → a day-by-day ledger, and what may not be said about it |
| `SecondWindowTests` | Which limit the second ring shows: the fullest in the headline's own model group, and the fallback when that group holds nothing more |
| `ZaiQuotaTests` | The GLM Coding Plan quota reply → windows, including a spend of zero being a reading rather than a gap |
| `ZaiErrorTests` | What the GLM Coding Plan's HTTP-200 refusals mean, from envelopes taken off both live hosts |
| `VolcengineParsingTests` | Ark's three reply shapes, from second-hand fixtures. [providers/volcengine.md](providers/volcengine.md) |
| `VolcengineProcessTests` | The `arkcli` subprocess: a stderr flood, an output flood, a child that ignores SIGTERM, one that closes its pipes and lives, descendant termination after the leader exits (with and without TERM handling), and how a non-zero exit is classified |
| `CommandCodeParsingTests` | Command Code's four replies → windows, from second-hand fixtures. Chiefly **which question the monthly row answers**: a running plan against its inferred, labelled grant; an account without one against the pool it bought; a plan the table cannot size against *nothing* — not zero, not the pool. Plus what absence may not be read as: absent credit pots are not an empty wallet, a summary that never arrived is not nothing spent, an answered `data: null` is not a failed lookup, and a ceiling of zero or less is not a limit already reached — each of those drew a wrong ring before it was a test. Also stable org ids across a reordered array, epoch-millisecond resets, `exceeded` outranking the arithmetic, and equal lengths not shuffling. [providers/command-code.md](providers/command-code.md) |
| `RailMoneyTests` | The figure a ring shows when a provider reports money: abbreviated past a thousand, truncated rather than rounded up, no early rollover at 999,999, the narrow currency symbol — and every form measured against the room the rail actually has, which is the bug it exists for. [providers/deepseek.md](providers/deepseek.md) |
| `ChromiumLocalStorageTests` | Origin filtering, sequence ordering and deletions, both text encodings, table prefix skipping, Snappy, 32KB padding and fragment assembly (including a zero-length FIRST). Malformed lengths pass through the real log/table parsers: overflowing varints, oversized key/value lengths, invalid block handles, truncated or undercounted batches, orphaned fragments, records crossing block boundaries, and values that would consume the restart array. All stores are synthetic. [providers/devin.md](providers/devin.md) |
| `ModelPriceAliasTests` | One model, several spellings: Grok Build's `-build` tag, a context window (`k3-256k`) being the same model, Kimi's abbreviations, Devin's dashed versions and effort suffixes, MiniMax's casing — and that an id nobody publishes a price for stays unpriced rather than being matched to something near it, since a loose match bills a model at another model's rate. [token-spend.md](token-spend.md) |
| `SpendSummaryTests` | Calendar spans and padded quiet days, excluding provider statistics from priced totals, agent/model ranking, streaks, peak hours and table sorting. Cross-midnight and resumed sessions contribute only their in-span priced buckets, so session and project totals agree with the selected span. Sessions without a project do not form an "other" bucket. [token-spend.md](token-spend.md) |
| `SpendSpanPreferenceTests` | Which span the Token spend pane opens on when nobody has chosen — a week — that every offered span round-trips through the store, that a chosen span is what a later instance is built with, and that a stored value the picker no longer offers falls back to the week. The store is an injected `UserDefaults` suite. [token-spend.md](token-spend.md) |
| `SpendReadingTests` | Default-off preference, streaming line boundaries and digest identity, mid-loop JSONL/SQLite cancellation, cancelled scans and cache writes, restart after cancellation, WorkBuddy binary exclusions, disk-cache revalidation/rescan, and the Codex cumulative parser independent of the pane switch. Synthetic stores and injected prices/cache locations. [token-spend.md](token-spend.md) |
| `SpendReadStateTests` | Window-scoped snapshot reuse on sidebar changes, empty-result and read-note retention, release when disabled/closed from another pane, explicit rescan, unfinished-read retry, and stale task completion/progress/cleanup fences. Production state, no real UI input. [token-spend.md](token-spend.md) |
| `SpendReadPerformanceTests` | Opt-in 138 MB synthetic WorkBuddy transcript through the production reader, exact tally assertion and process peak RSS report. Skipped unless `PULSE_SPEND_BENCHMARK=1`; run alone for memory comparisons. Evidence and command: [token-spend.md](token-spend.md#local-synthetic-measurement-2026-09-19) |
| `ModelSpendSummaryTests` | One model's drill-down through the production chain — reader quarter-hour buckets → `UsageLedgerReader.price` → `ModelSpendSummary.of`, on a fixed clock and a UTC calendar. Two models used on one day keep their own categories, hours and money; one display name sums several raw ids across several agents and agrees with the combined page's own model row; agent ties break by name; today, a week and all-time cut and pad on the calendar, the week's cutoff checked on both sides and all-time starting at the selected model's first use rather than a ledger's. A provider's statistics are excluded, an unpriced model keeps its tokens and categories, and a ledger with no per-model detail keeps its totals while `tally`, `hours` and `cost` come back nil. Hours reconcile **per agent, per day, per raw id**, so one agent's overcount cannot cancel another's missing detail and two days that swap tokens are withheld even where the totals match. Money is each kind at its own rate (a nil cache rate falling back to input), priced **per raw id** and summed, so two ids behind one name at different rates are never merged onto one; day, agent and category totals add to the model's, a part-priced name shows a subtotal beside `unpricedTokens`, a real zero rate is `$0` rather than nil, and missing or mismatched metadata is counted and never invented. [token-spend.md](token-spend.md) |
| `ModelSpendSummarySortingTests` | Model day-table ordering: missing categories or prices sort last in both directions, known zeroes sort numerically, fractional costs keep their `Double` precision while token columns compare as exact `Int` (two adjacent values above 2^53 are not collapsed), equal values use dates to break ties, and date/total columns preserve their own ordering. [token-spend.md](token-spend.md) |
| `SpendFormatTests` | USD estimate formatting in English and Chinese: true zero, a visible less-than-one-cent label for small positive amounts, and higher-precision hover values that retain tiny amounts and large amounts' fractional parts. Tests pass the locale explicitly. [token-spend.md](token-spend.md) |
| `AgentStoreTests` | The readers for agents that keep their work somewhere other than a Claude-shaped transcript: OpenCode's message JSON (reasoning counted as output, priced from `ModelPrices` and never from the store's own `cost`), Grok's per-model `modelUsage` winning over the flat totals beside it, Kimi's `input_other` being fresh input, and a store that is not there reading as absent rather than as an empty account. Every store is built by hand in its measured shape; nobody's transcripts are committed. [token-spend.md](token-spend.md) |
| `AgentCacheTests` | Nested transcript additions, appends and removals, including a **hidden** log in a hidden subdirectory; title metadata; SQLite WAL inputs with shared memory excluded; price-table fingerprints; a **multi-root** stamp that moves when either root's WAL commits while the database file stands still, and that separates an added or removed empty root while collapsing path aliases of one input; the production `save`/`load` mapping driven through an injected cache file, then `ModelSpendSummary`, so per-model categories, hours, money, origin, aggregate timing, unclassified tokens and the partial-counts flag survive a restart for a priced, an unpriced and a part-priced name; and the older `agent-1`–`agent-3` shapes refusing to decode. A real SQLite writer remains open across a committed insert to prove that a WAL update invalidates the fingerprint while the database file stays unchanged. [token-spend.md](token-spend.md) |
| `AgentSessionTests` | Synthetic OpenCode, Grok, Devin CLI and Kimi stores with work on two days, through the production reader and then the spend summary. Session buckets reconcile with reader totals; Today keeps only today's tokens and published-rate cost. [token-spend.md](token-spend.md) |
| `DevinSpendReconciliationTests` | Production dispatch over a synthetic native database and canonical/legacy ACP mirrors: a matched counted session enters the combined and model totals once; empty or undated database messages cannot suppress captures; unrelated sessions and independently read captures survive. [token-spend-sources.md](token-spend-sources.md) |
| `SessionTitleTests` | Title trimming, envelope rejection, both message-body shapes and directory fallback. Tests also run the real Claude transcript parser: a rename after the opening prompt is read, the last valid custom title wins, and token counts remain intact. [token-spend.md](token-spend.md) |
| `AgentUsageRecordTests` | The normalized boundary on synthetic records: increments rolling into days, sessions and slots; explicit-dedup-only folding; a bare total counted and never invented into a kind or priced; a partial split labelled against a broken one; a known-subset `isPartial` propagated to `hasPartialCounts` and shown as incomplete rather than complete; imported records entering the summaries while provider statistics do not; and aggregate timing landing on a day but never in an hour bucket. [token-spend.md](token-spend.md) |
| `AgentLogIOTests` | The shared helpers: a count is a whole, finite, in-range number and never a boolean, fraction or overflow; malformed JSON is a skipped line and never a partial value; a timestamp takes the unit the caller states; a file walk includes hidden logs and excludes `-shm`; and a missing database is opened read-only and not created. [token-spend.md](token-spend.md) |
| `SessionLogReadersTests`, `SessionLogReadersParsingTests` | Group A: the family catalogue and dispatch, the shared Pi-shaped parser and Prime's reconciliation, plus the non-Pi parsers — Gemini's three shapes, Qwen's documented cache relation, Amp's ledger/message reconciliation, Droid's aggregate settings and OpenClaw's stores — against original synthetic stores. [token-spend-sources.md](token-spend-sources.md) |
| `EditorLogReadersTests`, `EditorReaderChainTests`, `CommandCodeReaderTests`, `OpenCodeReviewReaderTests`, `ZCodeReaderTests`, `CherryStudioReaderTests`, `TencentBuddyReaderTests` | Group B: VS Code task logs, the Tencent JSONL-authoritative fallback, Cherry Studio's duplicate-export merge, and the standalone Command Code/OpenCode Review/ZCode readers, then the production chain from their records into the spend summaries. [token-spend-sources.md](token-spend-sources.md) |
| `DatabaseLogReadersTests` | Group C: Hermes, Goose, Zed, Kiro, Crush, Unsloth, Antigravity CLI, Micode and Devin Desktop against synthetic databases and event logs — the cost-only and estimate-only stores, Zed's zstd decode and its note, Antigravity CLI's excluded `#1` and partial records, and the ambiguous-cache stores. [token-spend-sources.md](token-spend-sources.md) |
| `StructuredLogReadersTests` | Group D's per-product JSON/JSONL stores, including a cumulative snapshot split into increments, Freebuff's estimate-only store, DSH's zstd frame handling and fork seed, the stores that keep output-with-reasoning once, and the stores that omit ambiguous reasoning and mark partial. [token-spend-sources.md](token-spend-sources.md) |
| `CapturedUsageReadersTests` | Group E's export/cache/capture readers and their `UsageImports/<client>` drop folder: Warp's tokens-are-zero contract, Cursor/Trae's per-file row multiplicity against cross-file multiset reconciliation with `isPartial`, the unknown-scope case with no declared account, Hindsight's untrusted-ledger skips, and Mcode's same-id-only merge. [token-spend-sources.md](token-spend-sources.md) |
| `CopilotLogReaderTests` | Copilot's three local sources and the OTEL → Desktop → VS Code filtering that stops one call being counted three times; OTEL records with no trace/response id and partial coverage of a desktop lifetime, desktop-ambiguous reasoning, and a VS Code request with no timestamp being skipped rather than dated at the epoch. [token-spend-sources.md](token-spend-sources.md) |
| `SpendAgentCatalogTests` | The catalogue is the seven verified readers plus the forty-seven added clients; every new client routes to a family and the six families are non-empty and disjoint; every routed client names at least one input root and routes to the same roots it declares; the legacy readers keep their verified default paths, their persisted `rawValue` and are not routed into the catalogue; and `requiresUsageExport`/`reportsTokenCounts`/`hasCapturedValidation` keep "recognised" from reading as "counted". [token-spend-sources.md](token-spend-sources.md) |
| `DevinParsingTests` | A provider read out of a file another app wrote: `used = 100 − remaining` on both windows, a paid plan's `-1` message counters being absent rather than an allowance of minus one, a free plan's message pool carrying no window, `hideDailyQuota` dropping a row rather than zeroing it, the live plan winning where two accounts share one store, and the launch stamp coming from the log directory's name — plus the endpoint half: a whole `Authorization:` line, a slug, an internal id and an organization URL all parsing into the right path segment, the candidate paths being ordered and unique, and the endpoint's percentages being **spent** where the saved plan's are **left**. [providers/devin.md](providers/devin.md) |
| `DevinSnapshotTests` | Saved-plan freshness, expired windows, balance-only expiry, and missing/future/unrecognized launch stamps. The direct-fetch cache path applies age/reset limits too; an idle app-cache route is not classified as an outage. All stores and timestamps are fixtures. [providers/devin.md](providers/devin.md) |
| `XiaomiMiMoTests` | A provider whose credential is assembled out of a browser store and whose refusals arrive inside an HTTP 200. The cookie filter keeps only the console's four names and drops analytics; both required names or nothing; a pasted `Cookie:` header; a control character refused rather than forwarded as a header injection; a repeated name taking the first rather than discarding the browser. Then the reply: a plan read across two routes, an empty `items` list being an account with **no plan** rather than one at 0%, an expired plan not being a current allowance, the usage route alone still drawing a ring, and a `code` 401 inside a 200 not being read as an answer. Plus the monthly window not claiming a length it was never told, and the mark actually loading. [providers/xiaomi-coding-plan.md](providers/xiaomi-coding-plan.md) |
| `CodexAppServerTests` | The helper's stdout closing. A `readabilityHandler` left on a pipe whose far end has gone is a busy loop, not a leak — it shipped as 290% CPU for eleven hours ([#25](https://github.com/qunqin24/Pulse/issues/25)). Covers the handler taking itself off at EOF, data still arriving before it, `shutDown` clearing it, and a stale pipe's EOF not tearing down the reader that replaced it. Driven against a plain `Pipe`, so it needs no `codex` installed. [providers/codex.md](providers/codex.md) |
| `RPCRequestLifecycleTests` | Codex and Kiro reconnects after a reply, server error, partial JSON followed by EOF, and explicit shutdown while waiting. The next helper answers after the old deadline but within its own. Also covers a real timeout, recovery, and completed requests releasing their client before the deadline. Uses isolated `/bin/sh` helpers and a shorter injected deadline; no installed CLI, PATH mutation, or real account. [providers/codex.md](providers/codex.md), [providers/kiro.md](providers/kiro.md) |
| `DevinAccountMixingTests` | Injected production service routes through cache reconciliation: different users and organizations cannot borrow readings; the same endpoint credential and organization can. Endpoint failures never cross to the app's saved plan, expired content cannot bypass scope checks, and scope plus newest-reading selection survive a cache restart. [providers/devin.md](providers/devin.md) |
| `DeepSeekParsingTests` | Where each denominator comes from, for the one provider that reports none: the watched mark advancing only on a rise, a peak of zero drawing nothing, a budget of nothing drawing nothing, money strings parsed with absent kept distinct from zero, which currency the ring follows, and `is_available` being the only thing that may say spent. [providers/deepseek.md](providers/deepseek.md) |

The release regressions also cover calendar-day buckets for aggregate and mixed-timing sessions (`AgentUsageRecordTests`), a cross-day Cursor cloud CSV session surviving a real cache reload without borrowing yesterday's usage (`CapturedUsageReadersTests`), rejection of pre-fix cache versions or missing session-day metadata (`AgentCacheTests`), and Command Code rewind retaining consumption with branch-local model attribution and replay deduplication (`CommandCodeReaderTests`).

## What is not, and why

Developer integrations also have `Integrations/raycast/tests/report.mjs`: run `npm test` from that extension directory after `npm ci`. It exercises the TypeScript consumer and launches the actual shell formatter with synthetic JSON, covering gaps, balance-only accounts, estimates, age, exact account selection and tmux escaping. `npm run build` and `npm run typecheck` check the Raycast extension. These are not evidence of real Raycast/sketchybar UI or macOS URL delivery; those need the host applications and a bundled Pulse. Installation: [integrations.md](integrations.md).

**No UI tests.** The panel is an accessory `NSPanel` whose hover cannot be driven by synthesised events — `hitTest` and synthetic `NSEvent`s both reported a handle as perfectly reachable while real clicks were being dropped, which is the lesson in [ui/input.md](ui/input.md). A UI test here would report the same thing.

**A rule test is not a chain test.** `AlertMemoryTests` hands the state machine readings directly, and two rounds of review missed a defect that lives *between* the service and the machine: the cache swaps a failure for cached figures and the reason is gone with it. Where a rule depends on something upstream, test it through that thing.

**No live provider calls.** Every route needs somebody's real credential and answers differently by plan. Fixtures are captured by hand from a real reply and committed; the capture is recorded in that provider's page.

**A fixture written from another project's parser, or from a vendor's own shipped client, is second-hand**, and has to say so where it lives. Volcengine's are, because nobody here holds that plan; Command Code's are written from the field names in its published npm bundle. A captured one replaces either the moment somebody with an account can produce one. Second-hand is enough to pin a shape against change, and not enough to claim the shape is right.

**The added agent readers are spec evidence, not machine evidence.** Their suites build the store by hand in the documented shape and read it back through the production reader; nobody's transcripts are committed, and none of the readers written from a compatibility target's second-hand format spec has been run against the product that writes it. A suite proves a reader parses the documented shape; it does not prove the shape is right. The suites assert the declared contract, not that every client has passed on a real machine, and a reader that reports `notes` — a compressed transcript it could not decode — is validated no further than the store the suite built. The seven clients read on a real machine keep that status, and every source is marked by evidence in [token-spend-sources.md](token-spend-sources.md).

**A subprocess test really spawns one.** `VolcengineProcessTests` runs `/bin/sh` on purpose: the two failures it covers — a child that fills the stderr pipe, and one that never exits — cannot be produced by a fake, and neither is visible by reading the code. The first version of that runner looked correct and had both; the *second* looked correct and still hung on a child that ignored SIGTERM. Neither was findable by reading. The deadline is a parameter so a test can use one second.

**The animated mark's motion is measured, not eyeballed.** Three of its properties cannot be seen in a screenshot and are read off the frames instead: that working pumps visibly harder than idle (peak-to-peak height of the drawn body), that a working mark actually throws ribbons within thirty seconds, and that no persona-and-mood pair slides the body off its canvas. All three were *failing* when written — rotation emphasis turned out to be swallowed by a spring, and two upstream states moved further than the viewBox has room for.

`BotMarkRestTests` uses the production `BotMarkProgramme.forMood`. It forbids sleeping, powering-down and bored in production and allows drowsy only for sleepy, across every mood, quiet/hover condition and weekday/weekend/day/night context. Two-minute simulations require zero resting-state frames for other personas and keep sleepy's night-time idle accent under a quarter. Low-eyelid time is bounded for sleepy; happy laughter and playful dizzy spins are not classified as sleep merely for narrowing their eyes. Night boundaries, awake entry and mood priority are checked separately. These are local engine-frame checks, not real panel input. [ui/rings-and-surface.md](ui/rings-and-surface.md#animated-marks)

`BotMarkChoreographyTests` prevents personalities from collapsing to the same state sets or just a different ordering: eight distinct idle signatures, distinct sets in every mood and attention response, and distinct work-finished acknowledgements. Everyday/attention states must also be disjoint from **every** persona's work states. Frame tests forbid idle ribbons, catch a live work ribbon before switching to idle, and ensure event particles stop when playback ends even if the observed event flag stays true. It drives complete production scenes to check every beat is visited in order and the head stays inside the rail's movement budget; checks catalogue coverage, safe state meanings, event duration/resumption, face-vs-morph duty cycle, the bouncing ball's drawn size, and expression-pool cycling. Its optional contact sheet uses `drawBotMark` through `ImageRenderer`, not a mock renderer or a claim of real AppKit input:

```bash
PULSE_BOT_PREVIEW=/path/to/existing/folder/bot-personalities.png swift test --filter renderContactSheet
```

**The animated mark is tested where it fails silently.** `BotMarkTests` covers the mood rule (busy outranks spent; no reading is unavailable), the persona rule (no character may play a state that contradicts the reading, and each wears a real, distinct shape), the colour deal (neighbouring rings never within 30° of hue, the same rail dealt the same way twice) and the contrast rule (no provider's body sinks into the disc, eyes always clear the body), plus that every state named anywhere exists in the bundled table and that the table is whole — 18 shapes of 96 points, 25 eye pairs, 39 states. None of those crash when they break: a mark that quietly reads `idle` about a signed-out provider, or a black body on a black disc, just looks like nothing. What the animation *looks like* is a screenshot question and is not tested.

**No network, no clock, no disk in a rule test.** `AlertMemory.alerts` takes `now` as an argument for exactly this reason. `UsageCache.init(file:)` takes a path for exactly this reason. Anything that has to reach for a real one is not a rule test.

## Two conventions

**The executable target is tested directly** (`@testable import Pulse`), not through a library split. Pulse is one app, not a framework with an app on top; carving seventy-eight files into two targets to make them reachable would be a refactor in service of the test runner. SwiftPM has allowed this since Swift 5.5.

**A symbol may be `internal` instead of `private` so a test can hold it**, and when it is, the comment says so and says not to tidy it back. `AntigravityUsageService.Reply` and `windows(from:)` are the first two. Nothing outside the module can see them either way; the difference is only whether the fixture test compiles.

## Fixtures

`Tests/PulseTests/Fixtures/`, copied whole into the test bundle so a schema change diffs readably. Read them with `Bundle.module.url(forResource:withExtension:subdirectory:)`.

Captured payloads carry no account name, email, or token — check before committing one. A quota reply is bucket ids, display names, fractions and reset times, and that is all it should be.

**A temporary fixture removes only the root it created.** A test asks for a unique root, writes its database and logs inside that root, and deletes just the root on the way out. It never calls `deletingLastPathComponent()` from a fixture path to find something to clean up: one level above a file in the system temporary directory is the system temporary directory, which is not the test's to remove. `AgentStoreTests` is the example — its `temporary(_:)` hands back the owned root, and every test defers a `removeItem` on exactly that URL.

`SessionPricingTests` checks unknown, mixed and published zero prices through normalized records, Claude/Codex transcripts, cache reloads and Today. It covers event and aggregate timing and rejects missing cache coverage. `AgentSessionTests` also exercises unpriced OpenCode, Grok, Devin and Kimi sessions. These are synthetic-store checks, not live-client validation.

`ProjectIdentityTests` covers same-name transcript directories through both cache layers, repeated paths across agents, source-only labels and Claude fallback folders. These are synthetic-store checks, not live-client validation.
